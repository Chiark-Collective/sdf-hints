# SDF Labeler Backend API

FastAPI backend for interactive SDF training data generation.

## Quick Start

```bash
# Install dependencies
uv sync

# Run development server
uv run uvicorn sdf_labeler_api.app:app --reload --port 8000
```

## API Documentation

When running, visit http://localhost:8000/docs for interactive API documentation.
